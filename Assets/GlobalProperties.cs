using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GlobalProperties : MonoBehaviour
{
    public static float tickLength = 0.001f;
    public static float conductionRate = 1f;

    public static float maxGlobalTemperature = 250f;
    public static float globalTemperatureIncreaseMod = .01f;
    public static float globalTemperature = 80f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
